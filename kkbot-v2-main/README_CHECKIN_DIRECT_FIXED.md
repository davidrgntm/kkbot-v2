# Checkin direct fixed

Bu versiyada route'lar to'g'ridan-to'g'ri `web_server.py` ichida ulanadi:

- `/checkin`
- `/checkout`
- `/check-in`
- `/check-out`
- `/web-checkin`
- `/web-checkout`
- `/verification`

Deploy logda chiqishi kerak:

```text
[web_server] verification routes applied: /checkin /checkout /verification
```
